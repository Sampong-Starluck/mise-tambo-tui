/**
 * Foundational infrastructure every backend builds on: {@code CliProcessRunner} (subprocess
 * execution) and {@code CancelRegistry} (tracking/cancelling live operations).
 */
@NullMarked
package com.sampong.tambo._common.base;

import org.jspecify.annotations.NullMarked;
